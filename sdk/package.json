{
  "name": "@sg32/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for sg32 — trustless Solana block proofs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@noble/hashes": "^1.7.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
